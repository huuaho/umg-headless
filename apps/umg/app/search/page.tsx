import { SearchContent } from "@umg/ui";

export const metadata = {
  title: "Search",
  description:
    "Search news coverage from United Media Group: Diplomatic Watch, Echo Media, and International Spectrum.",
};

export default function SearchPage() {
  return <SearchContent externalOnly />;
}
